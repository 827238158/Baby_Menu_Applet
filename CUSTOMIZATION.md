# 云端菜单维护指南

上云初始化完成后，菜单以 COS 中的云端资料为唯一真源。公开用户无需登录；“菜单管理”仅允许心愿夹现有两位白名单用户进入。

## 日常流程

1. 打开菜单页，长按店名标题（如“小画家厨房”）；白名单校验通过后进入菜单管理，访客长按无提示。
2. 在“菜品”“分类”“页面设置”中修改内容。
3. 点击保存，同步到两人共享的草稿，此时公开菜单不变。
4. 进入预览，检查菜品、规格、图片、分类和页面效果。
5. 确认修改概览后发布。其他用户下次进入或返回菜单页时自动检查新版本。

离开未保存表单会提示。另一位编辑者已保存时，本次保存返回冲突并保留本地表单；先记录需要保留的修改，再重新加载最新草稿并重新编辑。不能强制覆盖。预览后草稿发生变化则须重新预览再发布。

## 可编辑内容

- 菜品名称、简介、分类、标签、图片、上下架和顺序。
- 选择型规格：规格名、必选状态、选项名称与顺序；编辑名称保留已有 ID。
- 文字型规格：名称、提示和字数上限。分类“其他”由展示层自动生成，不重复写入云端菜品。
- 分类名称和顺序；删除分类时确认一并删除所属全部菜品，保存草稿并发布后公开菜单才更新。
- 店名、副标题、分享标题、头图、页面背景图、分享图片和头图位置。

菜品与分类通过页面底栏新增。调整头图时，在“页面设置”长按头图后拖动取景，松手保留当前位置，保存草稿并预览发布后生效；不再手填位置代码。

暂时不做的菜使用下架，保留资料方便恢复。价格固定为 `'0'`，规格不加价。不要通过重新建同名菜品代替修改已有菜品，以免改变稳定 ID。

## 图片

从手机拍照或相册选择 JPEG、PNG、WebP，单张不超过 8 MiB。上传原图后，SCF 调用数据万象生成最长边 800px、质量 75、自动回正且移除 EXIF 的 WebP。处理失败时保留表单并提示重新选图，不把原图当作缩略图保存。

COS 保持私有，记录只保存图片 Key；页面通过受控接口获取有时效的地址，必要时下载到微信临时路径。旧图仍可能被发布历史引用，不要在 COS 控制台按“当前菜单未使用”直接删除图片。

## 历史与恢复

在“发布历史”查看记录，选择一个历史版本恢复。恢复会替换共享草稿，需要确认，并且仍需预览发布才会影响公开菜单。全部已发布 JSON 及其图片一直保留；恢复后生成新版本，不覆盖旧发布。

失败发布留下的快照也受到图片保护；它不会出现在已发布历史链中。处理失败发布和异常写锁见 [部署恢复手册](docs/menu-cloud-deployment.md)。

## 本机旧资料备份

旧 Excel 转换和首次迁移工具已淘汰。Excel 与图片仅保留在本机备份目录，不参与运行、部署或 Git 跟踪；菜单资料以云端发布历史为准，不提供 Excel 回写。
