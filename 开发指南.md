# 开发指南

## 第一步：准备环境

提前安装：

- Git
- Node.js 18 及以上
- 微信开发者工具

提前准备：

- 微信小程序账号
- 腾讯云账号
- 私有 COS 存储桶
- 腾讯云 SCF 云函数
- 腾讯云数据万象（可选；使用图片功能时需要）


## 第二步：克隆项目

```cmd
git clone https://github.com/827238158/Baby_Menu_Applet.git
cd /d Baby_Menu_Applet
```

## 第三步：安装后端依赖

```cmd
cd /d serverless\gift-api
npm ci --omit=dev
cd /d ..\..
```

## 第四步：配置 COS 存储桶

在腾讯云 COS 控制台配置：

```text
访问权限：私有读写
地域：与 SCF 函数相同
版本控制：关闭或暂停
```

需要使用图片功能时，在数据万象控制台绑定该存储桶。

## 第五步：配置 SCF 运行角色

在腾讯云 CAM 创建 SCF 运行角色。

为目标存储桶配置以下权限：

```text
cos:GetBucket
cos:GetObject
cos:HeadObject
cos:PutObject
cos:PostObject
cos:DeleteObject
```

授权对象前缀：

```text
gift-folder/*
menu/*
```

为 `menu/` 的列举权限配置条件：

```json
{
  "string_equal": {
    "cos:prefix": "menu%2F"
  }
}
```

## 第六步：测试并打包 SCF

```cmd
cd /d serverless\gift-api

npm test
npm run check
npm audit --omit=dev

tar.exe -a -c -f gift-api.zip index.js src node_modules package.json
tar.exe -tf gift-api.zip
```

确认 ZIP 根目录包含：

```text
index.js
package.json
src/
```

返回项目根目录：

```cmd
cd /d ..\..
```

## 第七步：配置 SCF 函数

在腾讯云 SCF 控制台配置事件函数：

```text
运行环境：Node.js 18
执行方法：index.main_handler
内存：128 MB
超时：60 秒
地域：与 COS 相同
代码来源：上传 serverless/gift-api/gift-api.zip
运行角色：第五步创建的角色
```

添加环境变量：

```text
WX_APP_ID=微信小程序AppID
WX_APP_SECRET=微信小程序AppSecret
ALLOWED_OPENIDS=
SESSION_SECRET=至少32位随机字符串
COS_BUCKET=完整存储桶名称
COS_REGION=存储桶地域
COS_PREFIX=gift-folder
OPENID_DISCOVERY=true
LEGACY_PUT_UPLOAD_UNTIL=
```

## 第八步：创建函数 URL

在 SCF 函数详情中创建函数 URL：

```text
公网访问：开启
授权类型：开放
CORS：关闭
```

验证接口：

```cmd
curl https://你的函数URL/health
```

确认返回：

```json
{"data":{"status":"ok"}}
```

## 第九步：修改小程序配置

修改 `project.config.json`：

```json
{
  "appid": "你的微信小程序AppID"
}
```

修改 `miniprogram/config/gift-cloud.js`：

```javascript
module.exports = {
  // 填写 SCF 函数 URL，不保留末尾斜杠
  apiBaseUrl: 'https://你的函数URL'
}
```

## 第十步：获取管理员 OpenID

保持 SCF 环境变量：

```text
OPENID_DISCOVERY=true
ALLOWED_OPENIDS=
```

使用微信开发者工具导入项目。

让两个管理员分别进入一次心愿夹。

在 SCF 日志中搜索：

```text
[OPENID_DISCOVERY]
```

取得两个 OpenID 后修改环境变量：

```text
ALLOWED_OPENIDS=openid1,openid2
OPENID_DISCOVERY=false
```

重新部署 SCF 函数。

## 第十一步：创建首个云端菜单

旧 Excel 转换和首次迁移工具已淘汰。

使用白名单账号进入菜单管理页，创建菜单内容后依次执行：

```text
保存草稿
进入预览
发布菜单
```

## 第十二步：创建定时清理任务

在同一个 SCF 函数中创建两个 Timer 触发器：

```text
GiftImageCleanupDaily
MenuImageCleanupDaily
```

配置：

```text
执行频率：每天一次
执行时间：业务低峰期
目标函数：当前 SCF 函数
```

创建后分别执行一次控制台测试。

## 第十三步：配置微信合法域名

进入微信公众平台：

```text
开发管理 → 开发设置 → 服务器域名
```

添加：

```text
request 合法域名：SCF 函数 URL 域名
uploadFile 合法域名：COS 存储桶访问域名
downloadFile 合法域名：COS 存储桶访问域名
```

只填写域名，不填写路径。

## 第十四步：导入并发布小程序

打开微信开发者工具，选择“导入项目”，导入仓库根目录。

执行：

```text
清除缓存
重新编译
```

上传体验版本用于测试，完成功能验证后在小程序后台发布版本。
