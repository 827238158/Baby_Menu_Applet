# 设计记忆

本文件是本项目唯一的 UI / 视觉 / 交互规则来源。

## 产品定位

- 微信原生小程序菜单展示页，服务于手机端预览与发布展示。
- 不是完整点单系统，视觉上要像可浏览、可选择、可复制的菜单页，而不是支付或订单后台。

## 布局原则

- 商品列表保持手机端卡片布局。
- 商品图片固定尺寸，并使用 `aspectFill`。
- 商品名称、价格、标签、描述、规格提示和购物车控件不能重叠。
- 分类切换、商品展示、规格弹窗和购物车区域要适合单手浏览。
- 菜单页使用视口内纵向 flex 布局，工具栏、预览发布区和头图占固定内容高度，列表使用剩余高度，底部为固定购物车预留空间。

## 交互原则

- 商品卡片加购入口统一使用圆形 `+`；首次加购和步进器加号均使用几何线条绘制，以按钮圆心为交点，不依赖字体字形或文本基线。
- 有规格商品点击 `+` 打开底部规格面板，默认选中每组第一项。
- 无规格商品点击 `+` 直接加入购物车。
- 同一商品不同规格组合作为不同购物车项。
- 购物车单项数量限定为 1–99；从缓存恢复时以当前菜单重新校验规格并重建展示文案，不信任旧缓存的名称、价格或规格文案。
- 用户最终操作是复制已选菜单文本。
- 不要新增显式分享按钮、订单状态、订单编号、支付状态或提交订单接口，除非用户明确要求。

## 图片规则

- 线上菜单图片在 COS；文档引用稳定资产 ID，资产记录原图和 WebP 缩略图 Key，不保存临时签名 URL。
- 列表用缩略图，签名失效重新解析，内联失败用微信临时下载兜底；头图位置可配置，背景与分享图也按需下载。
- 图片为空或加载失败用本地占位资源；旧 assets/foods 仅用于迁移，禁止写 Windows 绝对路径。

## 菜单管理与预览

- 菜单公开读取，首次进入和返回时自动检查版本；首页不显示云端工具栏和刷新按钮，不启用下拉刷新。失败时展示最近成功缓存状态，无缓存显示重试，不复活随包旧菜单。空的已发布菜单是有效结果。
- 更新后重新校验购物车，失效菜品或规格移除并提示。
- 长按店名标题静默校验白名单，仅明确授权后进入管理。访客、网络失败无提示或跳转，预览页禁用隐藏入口。管理页和服务端继续独立鉴权。
- 管理分为菜品、分类、页面设置、发布历史；分类删除使用红色“确定”按钮，提示“删除该分类会将分类下所有菜品一并删除”，确认后同时移除草稿内分类和全部所属菜品，仍需保存并发布。
- 共享草稿显式保存，离开未保存表单提示。409 冲突保留本地表单，提示记录修改后重新加载，不能静默覆盖。
- 规格及选项改名保持 ID，支持单选和文字备注。价格固定字符串 '0'，不提供编辑价格及加价。
- 拍照/相册选图，上传和处理期间锁定表单以防图片写错行，失败保留编辑内容。
- 预览复用公开菜单 WXML/WXSS 和交互；独立页面屏蔽分享与心愿夹入口，购物车只驻留内存。发布显示概览并确认，旧修订被拒绝后要求重新预览。
- 历史可查看概要和恢复草稿；恢复前确认替换，公开菜单等再次发布才改变。全部快照及引用图片保留。

- 菜品卡片用右上角“编辑 / 关闭”切换内联编辑，关闭保留本地修改；基本信息、图片、规格与备注分区排列，图片点击更换，小型移除控件保留确认。排序时按稳定菜品 ID 保持编辑状态。
- 管理标题和说明同行底部对齐，说明可在右侧换行；编辑按钮按文字宽度加左右 12rpx 内边距。导航使用约 240ms 滑动底板，快速切换以最后选择为准。
- 菜品、分类页底栏依次为对应新增、重载、保存草稿、预览发布，四项等宽；页面设置和历史页保留后三项。仅预览发布使用强调色。新增后定位到新增项，菜品展开编辑。
- 不显示草稿同步状态和修订号，内部仍保留冲突校验和未保存标记；未保存退出使用原生确认，成功保存后解除提醒。
- 页面设置头图长按拖动取景，与首页共用等比覆盖计算和窗口布局；普通滑动滚动页面，拖动期间锁定滚动，松手保留草稿位置，触摸取消还原。不提供缩放旋转，图片未加载不可拖动，更换图片默认居中。

## 动效规则

- 菜品上下移采用约 240ms 相邻卡片换位，按实际展开高度计算位移；期间锁定结构操作，测量失败直接排序，支持减少动态效果。换位距离以相邻卡片尺寸为准，不受状态强调的 12rpx 限制。

- 整体采用克制、连续的 iOS 式微动效：动画用于解释按压、数量变化、内容出现、弹层进退和列表增删，不作为无意义装饰。
- 按钮反馈控制在 120–160ms，按下缩放约为 `0.94–0.98`；数量变化控制在 180–220ms，只让被操作的数字和购物袋轻微上弹。
- 卡片进入或状态强调控制在 220–260ms，位移不超过 `12rpx`；删除时先淡出并收起空间，再移除数据。
- 底部弹层和全屏图片使用 220–320ms 的淡入、位移或缩放，进入使用柔和 `ease-out`，退出使用 `ease-in`，保持来向与去向一致。
- 同一操作只动画直接相关元素，避免大幅旋转、闪烁、持续跳动或多个区域同时抢夺注意力。
- 支持 `prefers-reduced-motion` 时关闭非必要动画和过渡；本项目不使用振动或其他触觉反馈。

## 心愿夹

- 心愿夹入口位于菜单页头图右上角，使用现有白色描边和粉色强调体系；页面内包含“礼品夹 / 装修好物”两个分段，默认打开礼品夹。
- 页面使用固定紧凑头图和固定分段控件，只有下方列表在内部 `scroll-view` 中滚动；长列表不能把头图或分段控件推出屏幕。
- 点按分段时只让白色选中胶囊在浅灰轨道内平滑移动，不主动滚动页面，也不让列表做上下或横向位移动画；不支持横划切换。
- 礼品与装修好物共用 `collection-item-card` 横向卡片组件；有图时固定比例裁切，无图时分别显示礼物或房屋占位图标。
- 两个分段分别维护列表、总数、分页游标、本地缓存和列表滚动位置；装修好物首次切换时才加载，切回时恢复各自位置。
- 新增和编辑共用底部弹层，字段只包含图片、名称和简介，三项至少填写一项。
- 表单不显示关闭叉号，可通过顶部向下拖动或点击遮罩关闭；抽屉以向下滑出动画收场。图片、名称和简介仅在点击当前分段的底部保存按钮后同步，选图后先在表单内本地预览。底部操作区与表单保持同色，不使用分割线。
- 点击礼品图片打开沉浸式全屏预览；再次点击图片或底部退出按钮关闭预览，点击文字区域进入编辑；删除只放在编辑弹层并要求二次确认。
- 编辑弹层提供“移到礼品夹 / 移到装修好物”；移动后保留收藏项 ID、图片、名称、简介和创建时间，并切换到目标分段展示结果。
- 礼品按创建时间倒序排列，新增入口统一使用右下角圆形 `+`；加号使用水平、垂直线条绘制并与圆心几何居中，不依赖字体字形或文本基线。
- 心愿夹由两名授权用户共享，两区云端数据保存到同一私有 COS 中的隔离命名空间；本地缓存只能在白名单授权成功后用于展示地址复用和网络失败兜底。
- 未在白名单内的微信账号打开心愿夹时，仅展示“仅限受邀用户使用心愿夹”的受限提示和返回菜单入口，不展示任一分段的本地缓存。
- 礼品夹等待白名单校验时显示两张与礼品卡片同尺寸的浅色骨架，不显示缓存礼品、空状态或礼品数量；授权成功后，相同图片标识优先复用缓存展示地址，并关闭图片默认淡入效果。
- 礼品图片在线加载，必要时只下载到微信临时路径，不提供永久离线图片。
- 图片表单不显示“选填”或常驻来源按钮；点击空图区或预览图后，通过微信原生操作菜单选择“拍照”或“从相册选择”。拍照成功后在客户端支持时继续打开微信图片编辑器，编辑确认后才进入表单预览。已有图片的左上角显示半透明圆形叉号，二次确认后仅移除当前表单草稿图片。
- 礼品列表每页加载 20 条，触底无感续载；列表缩略图启用懒加载并使用数据万象持久化生成的 WebP，原图仅在全屏查看时请求。选图后表单仍直接预览本地原图，不等待云端缩略图。
- 不提供来源、价格、链接、复制清单或对外分享。
