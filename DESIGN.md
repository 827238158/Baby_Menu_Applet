# 设计记忆

本文件是本项目唯一的 UI / 视觉 / 交互规则来源。

## 产品定位

- 微信原生小程序菜单展示页，服务于手机端预览与发布展示。
- 不是完整点单系统，视觉上要像可浏览、可选择、可复制的菜单页，而不是支付或订单后台。

## 布局原则

- 商品列表保持手机端卡片布局。
- 商品图片固定尺寸，并使用 `aspectFill`。
- 商品名称、价格、标签、描述、规格提示和购物车控件不能重叠。
- 分类切换、商品展示、规格弹窗和购物车区域要适合单手浏览。

## 交互原则

- 商品卡片加购入口统一使用圆形 `+`。
- 有规格商品点击 `+` 打开底部规格面板，默认选中每组第一项。
- 无规格商品点击 `+` 直接加入购物车。
- 同一商品不同规格组合作为不同购物车项。
- 用户最终操作是复制已选菜单文本。
- 不要新增显式分享按钮、订单状态、订单编号、支付状态或提交订单接口，除非用户明确要求。

## 图片规则

- 商品图片放在 `miniprogram/assets/foods/`。
- `Dishes.imageFile` 只填文件名，留空使用占位图。
- 不要把 Windows 绝对路径写进图片配置。

## 礼品夹

- 礼品夹入口位于菜单页头图右上角，使用现有白色描边和粉色强调体系。
- 礼品列表沿用横向卡片布局；有图时固定比例裁切，无图时使用固定尺寸礼物占位图标。
- 新增和编辑共用底部弹层，字段只包含图片、礼品名称和简介，三项至少填写一项。
- 礼品表单只通过顶部向下拖动关闭，不显示关闭叉号，点击遮罩不关闭；有未保存修改时仍需确认放弃。
- 点击礼品图片预览大图，点击文字区域进入编辑；删除只放在编辑弹层并要求二次确认。
- 礼品按创建时间倒序排列，新增入口统一使用右下角圆形 `+`。
- 礼品夹只使用当前微信环境的本地缓存和本地文件，不提供来源、价格、链接、复制清单、分享或云同步。
