# 项目日志

仅记录重要操作、部署、迁移、恢复和长期决策；普通过程进度不写入本表。

| 日期 | 事项 | 摘要 |
| --- | --- | --- |
| 2026-07-26 | 初始化轻量记忆系统 | 新增 `DESIGN.md` 与 `memory/`，将项目边界、读取路由、运行命令、UI 规则和常见坑迁移到对应文件；保留 `README.md` 与 `CUSTOMIZATION.md`。 |
| 2026-07-29 | 部署礼品原图接口 | 将包含 `GET /gifts/{id}/image` 的新版 `gift-api.zip` 部署到腾讯云 SCF；用户真机确认原图正常，旧 404 的根因是线上仍运行旧包。 |
| 2026-08-02 | 礼品夹索引与上传架构升级 | 完成索引 schema v2、COS 防覆盖写锁、键集分页、幂等创建、POST Object 直传和每日孤儿图片清理，并部署到腾讯云 SCF。 |
| 2026-08-02 | 礼品前端与购物车一致性升级 | 补充缩略图 Key 契约、云端总数、分页去重、草稿 ID 重试、选图异步代次保护；购物车缓存升级为 v2 规范化 JSON 键，数量限制为 1–99。 |
| 2026-08-02 | 礼品后端依赖与生产配置 | `cos-nodejs-sdk-v5` 升级到 `3.0.0`，生产依赖审计无已知漏洞；确认 COS 版本控制未开启并备份关键前缀，SCF 角色增加 `cos:PostObject`，函数超时调整为 60 秒。 |
| 2026-08-02 | Timer 与上传真机验收 | 启用 `GiftImageCleanupDaily`；相册、拍照编辑、POST Object 直传、礼品保存、缩略图和原图预览完成真机验收，测试礼品随后删除。 |
| 2026-08-02 | 旧上传路由兼容决策 | `/uploads/presign` 默认关闭，仅保留最长 7 天显式兼容窗口；待两名用户均升级后删除该路由并重新部署。 |
| 2026-08-04 | 校准记忆文件职责 | `CURRENT.md` 恢复为单一当前快照，长期部署结果归入 `LOG.md`，稳定事实与路由留在 `MEMORY.md`，命令由 `RUNBOOK.md` 统一维护。 |
| 2026-08-04 | 替换项目记忆 Hook | 删除原 Node/PowerShell `CURRENT.md` 守卫，改用 `project-agent-bootstrap` 官方检索、匿名失败信号和结束归档门禁；按本项目领域定制路由，10 项单元测试、8 类场景模拟和安装器检查通过。 |
| 2026-08-04 | 心愿夹双分段与装修命名空间 | 本地完成“礼品夹 / 装修好物”双分段、独立缓存与分页；同一 SCF/COS 中新增 `gift-folder/decor/` 索引、图片和写锁命名空间，旧礼品路由和 Key 不变。开发者工具实测新路由返回 404，已确认本地 ZIP 包含路由，原因是线上 SCF 仍运行旧包；需上传部署 `gift-api.zip` 后再验收。 |
| 2026-08-04 | 收藏项跨分类移动决策 | 保留现有礼品、装修两份索引以避免自动迁移生产数据；新增固定双锁顺序、目标先写和幂等重试的移动接口。移动保留收藏项 ID 与图片 Key，删除和 Timer 清理必须跨两份索引判断图片是否仍被引用。 |
| 2026-08-04 | 上传新版心愿夹 SCF | 用户已上传包含装修好物和跨分类移动接口的新版 SCF，线上 `/health` 返回 200。部署后头图收藏数量仍正确，确认原礼品索引未丢失；卡片不可见的实际主因是微信开发者工具报 `Component is not found in path`，已将组件注册改为页面相对路径并补完整文件校验，滚动区明确高度作为并行布局加固，无需恢复 COS 数据。 |
| 2026-08-04 | 不增加旧版分类兼容层 | 心愿夹仅两名白名单用户使用，不让旧 `/gifts` 合并返回礼品与装修好物。收藏项移动后以云端两份索引的最新归属为准，两名用户需使用新版小程序查看两个分类。 |
| 2026-09-02 | 数据万象缩略图本地迁移 | 本地采用“小程序只上传原图、SCF 调用数据万象持久化生成 WebP 缩略图”的架构；固定最长边 800px、质量 75、自动回正和移除 EXIF，旧图不回填并暂留旧客户端缩略图直传兼容。尚未部署腾讯云 SCF、发布小程序或完成真机验收。 |
| 2026-09-05 | 菜单上云架构与本地实现 | 用户确认公开阅读、沿用两人白名单编辑、共享草稿手动发布、全历史保留。完成独立 menu/ 前缀、菜单后端模块、管理/预览页面及一次性迁移工具；菜单锁不自动抢占，清理保护草稿和全部快照。124 项本地测试通过并重新打包；尚未部署腾讯云、初始化菜单或发布小程序。 |
| 2026-09-05 | 部署菜单兼容 SCF | 小主已部署新 SCF 包，函数 URL 的 `/health` 可用；SCF 日志确认 `GET /menu` 返回 `MENU_NOT_PUBLISHED`，证明新菜单路由已上线且仅缺首次云端菜单初始化。COS 保持私有且版本控制关闭，数据万象已绑定；尚未初始化菜单、创建菜单 Timer、发布小程序或完成真机验收。 |
| 2026-09-05 | 创建并启用菜单图片清理触发器 | 小主创建 `MenuImageCleanupDaily`，Cron 为每日 03:30；首次菜单初始化完成后已启用。既有 `GiftImageCleanupDaily` 未改动并继续启用；菜单 Timer 控制台测试扫描 78 个对象，候选、删除和失败数均为 0。 |
| 2026-09-05 | 完成首次云端菜单初始化 | 使用一次性 STS 角色将旧菜单迁入私有 COS `menu/`：16 个分类、71 个菜品、39 张独立图片；生成草稿修订号 `1` 和首发版本 `r_58e060cbe9d7d8551614fca2d8803e69`。外部网络已验证匿名菜单及一个资产解析接口均返回 HTTP 200；临时环境变量、一次性角色和策略均已清除。COS 真机显示仍待后续验收。 |
| 2026-09-05 | 通过菜单合法域名与开发工具验收 | 微信公众平台已确认 SCF 域名配置在 `request`，COS 域名配置在 `request`、`uploadFile`、`downloadFile`；关闭开发者工具的域名校验绕过并清缓存编译后，云端菜单、头图和菜品图片仍正常显示，控制台无红色错误。真机权限和管理写入链路仍待验收。 |
| 2026-09-06 | 统一头图取景与加载链路 | 本地将头图从 CSS 背景改为可监听加载失败的图片层，管理拖动预览与首页共用取景计算；店铺图片独立加载并复用有效临时文件，保留既有位置字符串及草稿发布契约。未部署，实际灰屏及拖动效果仍待真机验收。 |
| 2026-09-06 | 菜单迁移资料移出代码包 | 旧 Excel 生成数据与图片统一迁至 `tools/menu-workbook/`，不再进入小程序包；心愿夹移除旧菜单数据依赖。被占用的旧背景图保留在源码目录，但通过 `packOptions.ignore`（以 `miniprogramRoot` 为根的 `assets/backgrounds`）排除，重新打开开发者工具后生效。 |
| 2026-09-07 | 淘汰旧菜单迁移资产 | 旧 Excel 转换、生成和首次迁移工具已删除；菜单首次内容改由白名单管理员在管理页创建、保存、预览并发布。Excel 与图片仅留本机备份并由 Git 忽略，重复的源码背景图及其打包忽略配置已移除。 |
