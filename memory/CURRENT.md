# 当前任务

## 任务

精简 `AGENTS.md` 中已由项目级 Hook 承担的 `CURRENT.md` 更新规则。

## 目标

- 删除 Hook 已覆盖的任务开始和任务结束更新要求。
- 保留 Hook 无法判断的执行中语义更新规则和记忆写入路由。
- 减少未来 Agent 默认上下文中的重复内容。

## 验收标准

- `AGENTS.md` 不再重复规定任务开始和结束时更新 `CURRENT.md`。
- `CURRENT.md` 的职责、执行中更新时机及其他记忆路由仍然明确。
- Hook 配置和脚本保持不变。

## 已知影响区域

- `memory/CURRENT.md`
- `AGENTS.md`

## 进度

- 已对照 Hook 提醒和阻断信息检查 `AGENTS.md` 的重复规则。
- 已确认任务开始与结束更新要求由 Hook 承担。
- 已确认执行中关键步骤、阻塞和方案变化仍需 Agent 语义判断。
- 已从 `AGENTS.md` 删除任务开始和结束时更新 `CURRENT.md` 的重复规则。
- 已将 `CURRENT.md` 职责与执行中更新时机合并为一条。
- 已确认旧规则文本无残留，`git diff --check` 通过。

## 下一步

本任务已完成，无剩余动作。
