# 当前任务

## 本轮任务

将已完成上云的菜单迁移资料移出小程序代码包，降低小程序包体积。

## 已完成

- 将旧生成数据和菜品图片移动至 `tools/menu-workbook/generated/` 与 `tools/menu-workbook/assets/`；运行时仅保留菜单缺图和分享兜底图。
- 心愿夹移除了对旧 `menu-data.js` 的导入，避免打包忽略后运行失败。
- 两张旧背景迁移图因当前进程占用暂保留在原目录，但已通过 `project.config.json` 的打包忽略规则排除；副本位于迁移资料目录。
- 更新生成器、一次性迁移工具、测试与维护文档，使迁移资料使用新位置；Excel 与生成数据只读检查通过（16 个分类、71 个菜品、39 张独立图片）。
- 105 项本地测试、JS 语法检查和 `git diff --check` 均通过。

## 验收与后续

- 微信开发者工具重新编译后，确认上传预览包不含 `assets/backgrounds`，菜单图片仍从云端正常加载，心愿夹可正常打开。
- 本轮代码与文档已纳入 `main` 的 Git 历史；远端同步状态以分支上游为准。尚未发布小程序，无新增云端数据、SCF 或 COS 变更。
